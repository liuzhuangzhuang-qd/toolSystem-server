{
  "name": "toolsystem-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "worker:convert": "node workers/conversion-worker.js",
    "cleanup:uploads": "node scripts/cleanup-uploads.js",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.2",
    "bullmq": "^5.34.5",
    "fastify": "^5.2.1",
    "fastify-plugin": "^5.0.1",
    "fluent-ffmpeg": "^2.1.3",
    "ioredis": "^5.4.2"
  }
}
