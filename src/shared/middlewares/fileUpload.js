/**
 * Reserved for multipart / upload constraints (e.g. user-upload feature).
 * @param {import("fastify").FastifyRequest} _request
 * @param {import("fastify").FastifyReply} _reply
 */
export async function validateUploadContext(_request, _reply) {
  return undefined;
}
